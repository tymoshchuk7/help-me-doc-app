import React, { ReactElement } from 'react';
import { PageLayout } from '../components';

const Main = (): ReactElement => {

  return (
    <PageLayout>
      Participants
    </PageLayout>
  );
};

export default Main;